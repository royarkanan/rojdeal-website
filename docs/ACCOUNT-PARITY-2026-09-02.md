# متابعة توحيد «حسابي» — 2026-09-02

المعرّف: `rojdeal-web-20260902-account-parity-review`. هذه جولة تطوير للموقع، وليست إعلاناً عن تطابق التطبيق والموقع بالكامل أو موافقة إنتاج.

## المصدر المؤكد

- استُرجع `RojDeal-Web-Review-Checkpoint-2026-09-02.zip`، نسخة الحفظ 7، من الملفات المتاحة. SHA-256 مطابق تماماً لملف التسليم السابق: `d76e2e1a39cc40e51e0f5befcc43176d2e01718ad52969cd871f85d1939fa988`.
- يحتوي المصدر السابق Next.js 15.5.24 ومعرّف `rojdeal-web-20260901-integrated-review`؛ تاريخ اسم ZIP لا يعني تغييرات إضافية في الكود.
- تمت المقارنة مع مصدر Flutter المرفق `RojDeal-App-Current(1).zip`، خصوصاً profile_screen وخدمات profiles وsaved_searches وtier.
- أوامر المستخدم الإضافية التي قال إنه سيُرسلها لم تصل. لذلك لا ندّعي دمج تعديلات أحدث بقيت على الماك فقط.
- لم تُعدّل ملفات Flutter أو هجراته أو Supabase أو DNS. لم يحدث نشر. مرجع التطبيق محفوظ كما هو، ولا حاجة لإعادة إدراجه بنسخة ZIP ثانية.

## ما أُضيف فعلياً

| الميزة | التنفيذ وحدود الإثبات |
|---|---|
| صفحة حسابي | الاسم والصورة ونوع الحساب وPRO/GOLD والتوثيق والإيقاف من `get_visible_profile`، مع إبقاء الإدارة وفق `adminAccess` الحقيقي |
| الباقة | بيانات `tier_plans` و`get_my_subscription_details`، الصلاحية والفوائد المترجمة ومدير الحساب. لا شراء أو ترقية أو تفعيل للاشتراكات المؤجلة |
| الإحصائيات | `get_my_listing_metrics`؛ مشاهدات ومفضلة، وحقول إضافية وفق analytics_level. الأرقام من الخلفية وليست أرقاماً مختلقة |
| البحث المحفوظ | عرض/فتح/حذف وتبديل التنبيهات من نفس `saved_searches`، مع قيد user_id. زر حفظ البحث في صفحة البحث |
| توافق الفلاتر | تحويل property/vehicle/other إلى real_estate/vehicles/miscellaneous وsale إلى sell مع المدينة والسعر؛ الصفر محفوظ. الأنواع المخصصة والفلاتر غير المدعومة لا تُسقط بصمت |
| المحظورون | `list_my_blocked_users` وإلغاء الحظر من `user_blocks` بقيدي blocker_id وblocked_id، مع تأكيد المستخدم |
| اللغة | الخيار أصبح داخل حسابي، باستخدام مبدّل اللغة الحالي؛ اللغة والمساعدة متاحتان أيضاً قبل تسجيل الدخول |
| المساعدة | روابط الدعم وكيفية الاستخدام والخصوصية والشروط والسلامة والمجتمع والحذف والتعريف، ودليل PDF نفسه الموجود في التطبيق دون إعادة تحريره |
| خصوصية الإعلانات | توضيح وضع الموقع الحالي بلا تتبع إعلاني أو تخصيص، ورابط سياسة الخصوصية. لا مفاتيح وهمية لمزود إعلانات غير مفعّل |
| الحساب والإشعارات | روابط مباشرة للإعلانات والرسائل والمفضلة والإشعارات وحذف الحساب الموجود؛ حماية حذف الموظف باقية |
| تعديل الملف | account_type الشخصي أصبح individual كما في التطبيق، إضافة صورة JPEG/PNG/WebP حتى 5MB تُحوّل إلى JPEG، وإحداثيات المكتب، وحقول معلّمة. لا حفظ نموذج فارغ بعد فشل تحميله |
| سلامة الواجهة | حواجز الحفظ المتكرر، فحص عدد الصفوف المعدلة، أخطاء محلية دون كشف تفاصيل SQL، إلغاء نتائج تحميل قديمة وإزالة بيانات الحساب السابق عند تبديل المستخدم |

## العقود المشتركة — لم تتغير قاعدة البيانات

الموقع أصبح مستهلكاً للعقود الموجودة في مصدر التطبيق:
`get_visible_profile(target_user)`, `get_my_subscription_details`, `get_my_listing_metrics`, `list_my_blocked_users`, `tier_plans`, `saved_searches`, `user_blocks`, `profiles`, `profile-avatars`.
لم نُنشئ RPC جديدة أو نعدّل RLS أو نسخ هجرات التطبيق إلى الموقع. يجب إثبات وجود وصلاحية هذه العقود في بيئة اختبار قبل اعتمادها، ولا يعني وجودها بالمصدر أنها مطبّقة في الإنتاج.

صور الحساب تُرفع باسم UUID جديد داخل مجلد المستخدم؛ الصورة القديمة لا تُحذف. عند فشل حفظ الملف بعد رفع صورة جديدة تُجرّب إزالة الملف الجديد وحده. تغيير metadata بعد حفظ profile له رسالة فشل جزئي مستقلة.

## اختبارات هذه الجولة

- TypeScript وESLint: ناجحان؛ لا تحذيرات ESLint تخص الكود.
- 33/33 اختبار دوال وخدمات بديلة ناجح؛ يتضمن 6 اختبارات جديدة للتحويل والتوافق وقيد المالك وفشل الصلاحية.
- بناء Next.js ببيانات mock: ناجح؛ 90 صفحة ثابتة ومسار account/[section] ديناميكي.
- 193 فحص HTTP محلي ببيانات mock: 77 فحص مسارات (بما فيها أقسام الحساب واللغات الأربع وnoindex و404 للمسار غير الصحيح وPDF) +116 فحص ترقيم/تذييل سابق.
- هذه ليست اختبارات دخول أو جلسات فعلية أو RLS أو رفع صورة أو تبديل حساب بالمتصفح. لا إثبات مرئي/لمسي على iPhone في هذه الجولة.
- بناء Cloudflare/OpenNext ببيانات mock: نجح وتولّد Worker؛ لم يُختبر Runtime ولم يحدث نشر. لا نشر لنسخة mock.

## ما بقي ولا يُعتبر منجزاً

1. اختبار رحلة حسابين حقيقيين في بيئة اختبار: الاسم/الصورة، الباقة وصلاحيتها، إحصائيات بلا تسريب، البحث المحفوظ في الاتجاهين، تغيير التنبيهات، إلغاء الحظر، حفظ الملف ورفض الكتابة غير الممنوحة.
2. تجربة تبديل المستخدم وتسجيل الخروج وفشل الشبكة على Safari/iPhone وChrome؛ اختبار اختيار الصورة وتحويلها ورفعها دون ضياع الصورة السابقة.
3. فلاتر البحث المحفوظ المشتركة لا تتضمن حالياً شجرة locationIds متعددة الاختيار أو governorate أو wanted أو transactionType أو ترتيب سعر مخصص. لا يُسمح بحفظها بصيغة تبدو متوافقة وهي ليست كذلك. دعمها يحتاج توسيع نموذج Flutter وقراءة/كتابة الطرفين واختبار التنبيهات.
4. تحسين ذكاء البحث ودقة النتائج متعددة اللغات وحد 500 مرشح وتقاطع نطاق ميزانية wanted: لم تُغلق في هذه الجولة.
5. التطابق الشامل لمسارات الإدارة والنشر والوسائط والمحادثات والخريطة، وتسجيل أحداث الإحصائيات من الموقع، يحتاج مراجعة واختبارات إضافية. لا نستنتج التطابق من شاشة حسابي وحدها.
6. تدقيق ترجمة الكردية بالكامل؛ دليل PDF من التطبيق كما هو، وليس محتوى أُعيد اعتماده لهذه النسخة.
7. صفحة الباقة تعرض الوضع الحالي فقط؛ الترقيات والدفع وتفعيل مزود إعلانات ما زالت مؤجلة وفق قرار الإطلاق السابق.
8. النواقص السابقة المتعلقة بحذف Staff والجدولة وصلاحيات الدعم وSMTP وOAuth والنشر والقانونيات والحصة باقية في PRELAUNCH-STATUS.md.

## القبول على الماك

افتح هذه الحزمة في مجلد مستقل؛ لا تستبدل المجلد الشغّال حتى اختبارها. احتفظ بملف البيئة على جهازك ولا ترفعه للمحادثة. لا تشغيل db push أو أوامر نشر ضمن فحص الواجهة.

الأوامر من داخل مجلد المشروع: `npm ci` ثم `npm run typecheck` و`npm run lint` و`npm test`. للتجربة ببيانات اصطناعية: `NEXT_PUBLIC_DATA_SOURCE=mock npm run build` ثم `npm run test:smoke`.

اختبار بيانات الحساب الحقيقية يحتاج إعدادات Supabase وبيئة اختبار معتمدة. نسخ .env.local الإنتاجي لا يُنشئ staging.
