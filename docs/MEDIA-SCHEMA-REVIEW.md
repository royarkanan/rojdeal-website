# مراجعة قيود الوسائط — مقترح فقط، لا موافقة إنتاجية

في مصدر التطبيق المرجعي schema.sql: CHECK ترتيب الصور0..11 ومدة الفيديو1..300. الكتالوج يسمح بحدود حتى30 صورة و1800 ثانية. لم نجد إزالة القيدين في migrations المرجعية. هذا دليل على المصدر، وليس فحصاً لقاعدة الإنتاج؛ قد تكون القاعدة الفعلية مختلفة.

الملف supabase/review-proposals/media-envelope.sql خارج migrations عمداً، وغير مضاف إلى مختبر الاختبار تلقائياً. لا تشغله على الإنتاج. يوسع السقف البنيوي فقط، ويتوقف إن لم يجد trigger فعّالاً يقرأ حدود القسم، أو وجد قيوداً غير متوقعة. لا يغير RLS أو Storage أو الأسعار أو حدود الكتالوج نفسها. لا يعطّل trigger عدد الصور أو الفيديو الواحد.

قبل اعتماده: افحص تعريف القيود والـtrigger في بيئة اختبار مطابقة، وراجع أن القيود لا تتضمن شروطاً إضافية خاصة بالإنتاج؛ اختبر الصور12/13/30/31 وفيديو300/301/1800/1801 ثانية، وأقساماً ذات حدود أصغر، وحساباً آخر لا يملك الإعلان. السكربت غير منفذ أو مختبر PostgreSQL هنا لغياب Docker/CLI. لا ندّعي صحة التطبيق الفعلي اعتماداً على اختبارات الخدمات المعزولة.

الأثر على التطبيق: يستخدم الجدول نفسه ويستفيد من السماح بحدود الكتالوج المعتمدة؛ لا تعديل لمصدر Flutter. التراجع إلى الحدود القديمة قد يفشل بعد وجود صفوف أعلى منها؛ افحص البيانات أولاً، ولا تحذف بيانات المستخدمين لتسهيل التراجع. تعديل CHECK يتطلب قفل الجدول أثناء التنفيذ، لذلك يلزم وقت نشر مناسب وموافقة صريحة بعد نجاح الاختبارات.
