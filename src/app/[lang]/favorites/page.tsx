import { FavoritesPanel } from "@/components/listings/FavoritesPanel";import {AccountBackLink} from "@/components/account/AccountBackLink";import {i18n,type Locale} from "@/lib/i18n-config";export default async function Page({params}:{params:Promise<{lang:string}>}){const{lang:r}=await params;const lang=(i18n.locales.includes(r as Locale)?r:i18n.defaultLocale) as Locale;return <div className="space-y-4 py-5"><AccountBackLink lang={lang}/><FavoritesPanel lang={lang}/></div>}
